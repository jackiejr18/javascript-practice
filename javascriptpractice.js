// Recursive Functions

// A recursive function in JavaScript is a function that calls itself 

function myFunction() {
  if(condition) {
    myFunction(); 
  }
  else {
    // stop calling recursion
  }
}

myFunction();